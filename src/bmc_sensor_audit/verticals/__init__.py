"""Verticals bundled with this distribution.

The BMC one ships here and loads through the same entry point an outside
vertical would use. That is the point: if the bundled one had a private path in,
the seam would be a naming convention rather than a door.
"""
